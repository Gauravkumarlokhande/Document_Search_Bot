# Document_Search_And_Virtual_Assitant
RAG Chatbot for document searching and summarization

Start the chatbot with following command:
``` 
poetry run uvicorn api.main:app --reload --host 0.0.0.0 --port 8080 
```